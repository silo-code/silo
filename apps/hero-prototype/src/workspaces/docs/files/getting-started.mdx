# Getting Started

Install the CLI and authenticate:

```
npm install -g acme-cli
acme login
```

Then create your first project:

```
acme init my-project
```

See the [API reference](/docs/api-reference) for what's available once
you're authenticated.
