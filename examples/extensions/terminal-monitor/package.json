{
  "name": "terminal-monitor",
  "displayName": "Terminal Monitor",
  "version": "0.1.0",
  "description": "Surfaces all terminal sessions in a dedicated side panel and workspace rows, with per-terminal status badges on tabs.",
  "private": true,
  "type": "module",
  "silo": {
    "id": "silo.terminal-monitor",
    "engine": "^0.9",
    "main": "dist/index.js",
    "permissions": []
  },
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@silo-code/sdk": "workspace:*",
    "@types/react": "^19.0.0",
    "esbuild": "^0.24.0",
    "react": "^19.0.0",
    "typescript": "^5.6.0"
  }
}
