{
  "name": "notify-demo-extension",
  "displayName": "Notification Demo",
  "version": "0.1.0",
  "description": "A command-triggered playground for Silo's toast notifications and modals (ctx.ui.notify / showModal / confirm / prompt / showMenu).",
  "private": true,
  "type": "module",
  "silo": {
    "id": "silo.notify-demo",
    "engine": "^0.1",
    "main": "dist/index.js"
  },
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "esbuild": "^0.24.0",
    "react": "^19.0.0",
    "typescript": "^5.6.0"
  }
}
