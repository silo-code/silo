{
  "name": "agent-inspector",
  "displayName": "Agent Inspector",
  "version": "0.1.0",
  "description": "Read-only inspector panel over ctx.agents — activity, staleness, and resume hints per terminal. Demonstrates the experimental ctx.agents surface (RFC 0017).",
  "private": true,
  "type": "module",
  "silo": {
    "id": "silo.agent-inspector",
    "engine": "^0.9",
    "main": "dist/index.js",
    "permissions": []
  },
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@silo-code/sdk": "workspace:*",
    "@types/react": "^19.0.0",
    "esbuild": "^0.24.0",
    "react": "^19.0.0",
    "typescript": "^5.6.0"
  }
}
