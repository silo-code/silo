// @silo-code/ui — internal shared component library (PRIVATE, never published;
// the component library is not a public extension surface — see REPO-STRATEGY).
// Placeholder: shared components are extracted here as they emerge. Until then
// this package reserves the name + the slot in the workspace graph.
export {};
