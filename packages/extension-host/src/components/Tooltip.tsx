// The host owns the CSS; the component logic lives in the public SDK so
// extensions can import Tooltip from "@silo-code/sdk" directly.
import "./Tooltip.css";
export { Tooltip } from "@silo-code/sdk";
